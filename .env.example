# MongoDB connection string
# Replace the values with your actual MongoDB credentials
MONGODB_URI=mongodb+srv://username:password@cluster.example.mongodb.net/?retryWrites=true&w=majority&appName=YourAppName

# MongoDB database name (optional - defaults to 'personal-finance' if not specified)
MONGODB_DB_NAME=personal-finance

# Node environment (development, production, test)
NODE_ENV=development

# Add any other environment variables your application needs
# Example:
# API_KEY=your_api_key_here
# SECRET_TOKEN=your_secret_token_here
